# 백업본과 작업용 원본 이미지는 올리지 않는다 (서버에 배포되는 팩만 추적)
*.bak
*_backup.zip
*.png

# 창 그림은 원본이므로 예외로 추적한다 (gui-pack.ps1 이 이것을 읽는다)
!gui/*.png

# 배포하지 않는 작업용 팩 (치장·도구 모양 원본 보관함)
RR.zip

# 이름을 붙이는 중인 도구 모양 임시 보관함 (배포 대상 아님)
tools-pending/
